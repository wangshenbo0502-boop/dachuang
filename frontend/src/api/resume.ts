/**
 * 文件名称：resume.ts
 * 文件作用：简历优化相关 API 请求封装。
 * 当前阶段仅定义请求函数框架，具体接口后续实现。
 */

import http from "./index";

// TODO: 提交简历进行优化
export function submitResume(data: object) {}

// TODO: 获取优化结果
export function getOptimizeResult(id: string) {}

// TODO: 导出简历
export function exportResume(id: string, format: string) {}
